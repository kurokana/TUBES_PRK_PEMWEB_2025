# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=sipamali_db

# Application Settings
APP_ENV=development
BASE_URL=http://localhost/kelompok_22/

# Upload Settings
MAX_FILE_SIZE=5242880
UPLOAD_DIR=uploads/

# Session Settings
SESSION_LIFETIME=7200

# Security
# Generate new secret key for production:
# php -r "echo bin2hex(random_bytes(32));"
APP_SECRET=change-this-in-production
